# frontend/AGENTS.md

Frontend-specific rules:
- Prefer scoped UI changes over shared design-system rewrites.
- Avoid changing routes, navigation structure, or state boundaries unless the task explicitly requires it.
- Keep visual and naming consistency with existing patterns.
- Validate with the frontend build, test, lint, and typecheck commands from `.ai/project.yaml` where relevant.
