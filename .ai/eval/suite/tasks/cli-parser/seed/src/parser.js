// Implement parseArgs(argv, spec) per the task specification.
// Keep this module name and export shape: tests require ../src/parser.js.

function parseArgs(argv, spec) {
  throw new Error('not implemented');
}

module.exports = { parseArgs };
