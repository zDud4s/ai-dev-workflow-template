# USD per 1,000,000 tokens. cached_input = prompt-cache READ price (~10% of input, both vendors).
# Claude: claude-api skill model catalog (cached 2026-06-04). Codex: OpenAI API pricing, June 2026 (web-verified).
# Verified 2026-06-27. NOTE: codex emits a single "tokens used" total (stored as tokens_out), so codex
# real cost is priced at the OUTPUT rate — an upper bound, since the total also includes input/reasoning tokens.
#
# LONG-CONTEXT TIER (NOT modeled — flat rates below are the standard tier):
#   gpt-5.5: prompts > 272K INPUT tokens bill at 2x input ($10) + 1.5x output ($45) for the whole session.
#   gpt-5.4: similar context tiers apply. Claude (opus 4.x) has NO long-context premium — flat to 1M.
#   Left un-modeled deliberately: codex rows carry no tokens_in (one total only), so the >272K INPUT
#   threshold can't be detected for them, and no ledger row comes near it (largest codex total ~186K).
#   If claude phases later log >272K-input prompts, OR codex tokens_in capture lands, add tiered logic then.
claude-opus-4-8:   { input: 5.0,  output: 25.0, cached_input: 0.5 }
claude-fable-5:    { input: 10.0, output: 50.0, cached_input: 1.0 }
claude-opus-4-7:   { input: 5.0,  output: 25.0, cached_input: 0.5 }
claude-opus-4-6:   { input: 5.0,  output: 25.0, cached_input: 0.5 }
claude-sonnet-4-6: { input: 3.0,  output: 15.0, cached_input: 0.3 }
claude-haiku-4-5:  { input: 1.0,  output: 5.0,  cached_input: 0.1 }
gpt-5.5:           { input: 5.0,  output: 30.0, cached_input: 0.5 }
gpt-5.4:           { input: 2.5,  output: 15.0, cached_input: 0.25 }
gpt-5.4-mini:      { input: 0.75, output: 4.5,  cached_input: 0.075 }
